import { MetadataRoute } from "next"; import { caseStudies, posts } from "@/content/data"; import { site } from "@/lib/site";
export default function sitemap():MetadataRoute.Sitemap{const blogPages=Array.from({length:Math.max(Math.ceil(posts.length/9)-1,0)},(_,i)=>`/blog/page/${i+2}`);const pages=["","/website-maintenance","/web-development","/web-design","/case-studies","/blog",...blogPages,"/contact"];return [...pages.map(p=>({url:`${site.url}${p}`,lastModified:new Date()})),...caseStudies.map(c=>({url:`${site.url}/case-studies/${c.slug}`,lastModified:new Date()})),...posts.map(p=>({url:`${site.url}/blog/${p.slug}`,lastModified:new Date(p.date)}))]}
